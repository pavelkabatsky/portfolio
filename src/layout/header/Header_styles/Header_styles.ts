import styled from "styled-components";

const Header = styled.header`
  padding: 16px 0;
  position: fixed;
  left: 0;
  right: 0;
  top: 0;
  background-color: #ffffffd8;
  z-index: 999999;
`;

export const S = {
    Header
}